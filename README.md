# 时间开销记录仪网页应用

一款极简的时间跟踪工具，支持快速记录日常活动时间，并生成可视化报告。

## 功能特性

- ⚡ **快速记录**：点击空白块即可记录时间（默认30分钟）
- 🎨 **分类管理**：支持自定义活动分类（正/负/平属性）
- 📊 **数据可视化**：自动生成日/周/月报，图表展示时间分配
- 🔒 **单用户模式**：无需登录，直接使用
- 📱 **响应式设计**：适配桌面、平板、手机

## 技术栈

### 前端
- React 18 + Hooks
- Redux Toolkit（状态管理）
- Material-UI（UI组件库）
- ECharts（数据可视化）

### 后端
- Node.js + Express
- MongoDB（数据库）

## 快速开始

### 环境要求
- Node.js 18+
- MongoDB 5.0+
- npm 或 yarn

### 安装依赖

```bash
npm run install-all
```

### 配置环境变量

复制 `env.example` 为 `.env` 并修改配置：

```bash
# Windows
copy env.example .env

# Linux/Mac
cp env.example .env
```

然后编辑 `.env` 文件，设置 MongoDB 连接和 JWT 密钥。

### 启动开发服务器

```bash
npm run dev
```

前端运行在 http://localhost:3000
后端运行在 http://localhost:5000

## Docker 部署

### 使用 Docker Compose

```bash
docker-compose up -d
```

### 构建镜像

```bash
docker build -t time-tracker-app .
```

## 文档

- [API 接口文档](docs/API.md) - 完整的 API 接口说明
- [部署指南](docs/DEPLOYMENT.md) - 详细的部署步骤和配置说明

## 项目结构

```
.
├── client/          # React 前端应用
│   ├── src/
│   │   ├── components/  # React 组件
│   │   ├── store/       # Redux store
│   │   ├── services/    # API 服务
│   │   └── utils/       # 工具函数
├── server/          # Node.js 后端服务
│   ├── models/      # MongoDB 模型
│   ├── routes/      # API 路由
│   ├── middleware/  # 中间件
│   └── controllers/ # 控制器
└── docs/            # 文档
```

## 开发说明

### 项目结构

```
.
├── client/              # React 前端应用
│   ├── public/          # 静态文件
│   ├── src/
│   │   ├── components/  # React 组件
│   │   │   ├── Login.js          # 登录/注册页面
│   │   │   ├── Dashboard.js      # 数据看板
│   │   │   ├── TimeGrid.js       # 时间轴组件
│   │   │   └── CategoryManager.js # 分类管理
│   │   ├── store/       # Redux store
│   │   │   ├── store.js          # Store 配置
│   │   │   └── slices/           # Redux slices
│   │   ├── services/    # API 服务
│   │   │   ├── api.js            # Axios 配置
│   │   │   ├── authService.js   # 认证服务
│   │   │   ├── timeRecordService.js # 时间记录服务
│   │   │   ├── categoryService.js   # 分类服务
│   │   │   └── reportService.js    # 报表服务
│   │   └── utils/       # 工具函数
│   └── package.json
├── server/              # Node.js 后端服务
│   ├── models/          # MongoDB 模型
│   │   ├── User.js      # 用户模型
│   │   ├── Category.js  # 分类模型
│   │   └── TimeRecord.js # 时间记录模型
│   ├── routes/          # API 路由
│   │   ├── authRoutes.js
│   │   ├── timeRecordRoutes.js
│   │   ├── categoryRoutes.js
│   │   └── reportRoutes.js
│   ├── controllers/     # 控制器
│   ├── middleware/      # 中间件（认证等）
│   ├── config/          # 配置文件
│   ├── utils/           # 工具函数
│   ├── index.js         # 服务器入口
│   └── package.json
├── docs/                # 文档
│   ├── API.md           # API 文档
│   └── DEPLOYMENT.md    # 部署指南
├── docker-compose.yml    # Docker Compose 配置
├── Dockerfile           # Docker 镜像配置
└── package.json        # 根目录 package.json
```

### 主要功能模块

1. **时间记录**
   - 点击时间块快速记录
   - 支持自定义持续时间
   - 添加备注和标签

2. **分类管理**
   - 创建/编辑/删除分类
   - 设置分类颜色和类型（正/负/平）
   - 默认分类预设

3. **数据可视化**
   - 日报/周报/月报
   - 饼图展示时间分配
   - 折线图展示趋势
   - 关键指标统计

## 许可证

ISC

